# Phase 22 V9 — Storefront Root Cause + CMS Sliders

Fixes the blank storefront caused by missing media/icon constants. Adds live-product spotlight inside the circular hero, a real CMS banner carousel after Hero, and preserves the real Supabase product slider. No new SQL is required if V8 SQL was already run.
